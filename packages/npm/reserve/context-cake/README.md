# context-cake

This version reserves the hyphenated name against typosquatting. It contains no
code, and it is not the package to install.

Install [`contextcake`](https://www.npmjs.com/package/contextcake) instead:

```sh
npm install -g contextcake
```

<https://contextcake.com>
