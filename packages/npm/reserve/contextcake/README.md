# contextcake

This version reserves the name. It contains no code.

The ContextCake CLI publishes from
[ContextCake/context-cake](https://github.com/ContextCake/context-cake) with npm
trusted publishing, so every real version carries provenance. Install a released
version:

```sh
npm install -g contextcake
```

<https://contextcake.com>
