---
// Changelog — generated from GitHub releases at build time (design.md §6).
// TODO(agent): build-time fetch of releases with a graceful empty state.
// Unauthenticated fetch only; while the repo is private this stays empty.
import Base from '../layouts/Base.astro';
---

<Base title="ContextCake changelog" description="Release notes for ContextCake.">
	<main>
		<h1>Changelog</h1>
		<p>No releases yet.</p>
	</main>
</Base>

<style>
	main {
		max-width: 42rem;
		margin: 0 auto;
		padding: 3rem 1.5rem;
	}
</style>
